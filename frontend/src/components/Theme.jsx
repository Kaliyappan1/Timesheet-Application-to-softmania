import { createTheme } from '@mui/material'

const theme = createTheme({
    palette: {
        primary: {
           main: "#0d840b",
        },
        secondary: {
            main: "#004201",
            light: "#fff"
        },
        
    },
  
})


export default theme;